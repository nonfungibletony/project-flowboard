feat(workspaces): implement workspace entity and CRUD UI (#42 #49)

Backend
- Add workspaces table to schema (id, name, slug, created_by, timestamps)
- Add workspace_id nullable FK to boards with CASCADE delete
- Migration 0002_add_workspaces.sql + journal entry
- New API route: /api/workspaces (GET, POST, GET /:id, GET /:id/boards, PATCH, DELETE)
- Register /workspaces in API entrypoint

Shared
- Add WorkspaceSchema, CreateWorkspaceSchema, UpdateWorkspaceSchema

Frontend
- New hook: useWorkspaces (load, create, update, delete with optimistic updates)
- Rewrite Workspaces page with real API: list, create inline, rename inline, delete
- Sidebar: display workspace list under header with links to /workspaces/:id
- vite.config.ts: allow flowboard.castiel.com.au in preview.allowedHosts (#50)

Closes #42
Closes #49
